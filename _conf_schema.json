{
  "decision_mode": {
    "description": "互动决策模式",
    "type": "string",
    "default": "legacy",
    "options": [
      "legacy",
      "persona_model"
    ],
    "hint": "旧安装保持 legacy。persona_model 沿用当前 AstrBot 人设，每个完整回合先模型决策，再由主 Agent 回复；建议先打开 shadow_mode。"
  },
  "decision_provider": {
    "description": "互动决策 Provider",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "仅 persona_model 生效；为空沿用主回复 Provider。每轮至多一次决策调用，决策模型不调用工具。"
  },
  "decision_timeout": {
    "description": "决策超时秒数",
    "type": "float",
    "default": 8.0,
    "hint": "范围 1~30 秒，包含等待全局调用名额；不重试。环境消息超时静默，明确求助保守交给 Agent。"
  },
  "decision_backend": {
    "description": "决策层后端",
    "type": "string",
    "default": "model",
    "options": [
      "model",
      "jev",
      "laya"
    ],
    "hint": "仅 persona_model 生效。model 用当前聊天模型按 JSON 决策；jev 改由 TypeSafe System One（Jev）决策模型答题；laya 用自建的 Laya typed-decision 服务答题。jev 与 laya 回答的题目与映射规则完全相同，一次调用回答「是否开口、怎么回、什么状态、多长、为什么」，选项全部由本插件给定，低于置信度门槛就不采用并回落到本地保守计划。"
  },
  "jev_base_url": {
    "description": "Jev 接口地址",
    "type": "string",
    "default": "https://api.typesafe.ai",
    "hint": "TypeSafe 官方地址；也可填 OpenRouter（https://openrouter.ai/api）、Vercel AI Gateway（https://ai-gateway.vercel.sh/typesafe）或自建的兼容服务。只接受 http(s)、不带账号密码与查询串的地址；填写密钥时非本机地址必须用 https，否则降级为不调用。"
  },
  "jev_model": {
    "description": "Jev 模型 ID",
    "type": "string",
    "default": "jev-latest",
    "hint": "默认别名 jev-latest 指向最新稳定版。调过置信度门槛后建议钉住具体版本（如 jev-1.13.0），别名会随新版本移动；经网关时按各家写法填写，例如 OpenRouter 的 ~typesafe/jev-latest。"
  },
  "jev_api_key_env": {
    "description": "Jev API Key 环境变量名",
    "type": "string",
    "default": "TYPESAFE_API_KEY",
    "hint": "填写运行 AstrBot 的进程环境变量名，不是密钥本身；凭据只从进程环境读取，不写配置、不进面板、不进日志。变量名须与 Jev 相关（含 JEV、TYPESAFE、OPENROUTER、GATEWAY、AIMLAPI 或 CHAT_DYNAMICS），否则回退默认值并在配置提示中说明。"
  },
  "jev_timeout": {
    "description": "Jev 决策超时秒数",
    "type": "float",
    "default": 6.0,
    "slider": {
      "min": 1,
      "max": 30,
      "step": 0.5
    },
    "hint": "范围 1~30 秒，只包住这一次 HTTP 调用，整体仍受决策超时约束。Jev 通常百毫秒级返回；不重试，超时即回落本地保守计划。"
  },
  "jev_min_confidence": {
    "description": "Jev 决策置信度门槛",
    "type": "float",
    "default": 0.6,
    "slider": {
      "min": 0.3,
      "max": 0.95,
      "step": 0.05
    },
    "hint": "范围 0.3~0.95。取「方式、状态、长短、理由」四项置信度的最小值，低于门槛就不采用这次判断：点名消息按本地保守计划回应，环境消息继续旁听。官方建议 0.6 以下不要自动执行。"
  },
  "laya_base_url": {
    "description": "Laya 接口地址",
    "type": "string",
    "default": "http://127.0.0.1:8900",
    "hint": "自建 Laya typed-decision 服务的地址，POST /predict 一次回答与 Jev 完全相同的一组问题。只接受 http(s)、不带账号密码与查询串的地址。走明文 http 时只放行本机与内网网段（10/8、172.16/12、192.168/16、链路本地等），公网地址必须用 https——这里发出去的是群聊内容本身，不只是密钥。"
  },
  "laya_timeout": {
    "description": "Laya 决策超时秒数",
    "type": "float",
    "default": 1.5,
    "slider": {
      "min": 0.1,
      "max": 10,
      "step": 0.1
    },
    "hint": "范围 0.05~30 秒，只包住这一次本地 HTTP 调用，整体仍受决策超时约束。Laya 稳态约 20~60 毫秒，1.5 秒已能容纳一次冷启动；不重试，超时即回落本地保守计划。"
  },
  "laya_min_confidence": {
    "description": "Laya 决策置信度门槛",
    "type": "float",
    "default": 0.6,
    "slider": {
      "min": 0.3,
      "max": 0.95,
      "step": 0.05
    },
    "hint": "范围 0.3~0.95，语义与 Jev 置信度门槛一致：取四项置信度的最小值，低于门槛就不采用这次判断并回落本地保守计划。"
  },
  "vibe_backend": {
    "description": "氛围分类后端",
    "type": "string",
    "default": "llm",
    "options": [
      "llm",
      "laya"
    ],
    "hint": "决定谁来读群氛围（快嘴玩梗 / 认真探讨 / 冷场衰退）。llm 用氛围模型按单标签分类；laya 改用自建的 Laya 决策服务，在封闭三选项里直接选一，没有自由文本要解析。与决策层后端分开配置：氛围是低频校准，回合决策是每回合都要跑，两者常常想要不同的选择。"
  },
  "vibe_min_confidence": {
    "description": "氛围分类置信度门槛",
    "type": "float",
    "default": 0.55,
    "slider": {
      "min": 0.3,
      "max": 0.95,
      "step": 0.05
    },
    "hint": "范围 0.3~0.95，仅 vibe_backend=laya 生效。低于门槛就不采用这次校准，沿用当前氛围判定。门槛比决策层低一些：氛围是「读」不是「做」，而且后面还有迟滞状态机吸收误判。"
  },
  "reply_timeout": {
    "description": "回复模型总超时秒数",
    "type": "float",
    "default": 60.0,
    "hint": "范围 5～300 秒，包含 Provider 查找和完整模型请求；兼容旧版 Provider。超时取消请求并释放当前生成任务。"
  },
  "tool_agent_timeout": {
    "description": "原生 Agent 总超时秒数",
    "type": "float",
    "default": 120.0,
    "hint": "范围 5～600 秒，包含媒体准备、请求钩子、Provider 查找和工具执行。与决策超时独立，超时取消当前 Agent。"
  },
  "enable": {
    "description": "是否启用群聊动态过滤器",
    "type": "bool",
    "default": true,
    "hint": "开启后，对生效范围内的群聊做防抖、指代门闩、冷却和回复装饰。主回复仍走 AstrBot 原管线（人格、历史、工具）。"
  },
  "pipeline_mode": {
    "description": "管线模式",
    "type": "string",
    "default": "filter",
    "options": [
      "filter",
      "exclusive"
    ],
    "hint": "filter：只关门/装饰，不 stop_event，其它插件仍能收到消息。exclusive：旧独占模式，会吞掉后续插件，仅在确实需要时开启。"
  },
  "ambient_intervention": {
    "description": "允许未被点名时主动插话",
    "type": "bool",
    "default": false,
    "hint": "默认关闭。legacy 下按 WTS 决定弱指代是否开口。persona_model 下该开关不再单独否决模型决策，仅作控制台兼容项。"
  },
  "takeover_all": {
    "description": "对全部群聊生效",
    "type": "bool",
    "default": false,
    "hint": "开启后对所有未被黑名单排除的群启用过滤器。默认关闭，避免一安装就改变全部群聊。"
  },
  "takeover_groups": {
    "description": "生效群聊白名单",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": [],
    "hint": "填入需要过滤的群聊 ID。白名单为空且未开启“对全部群聊生效”时，不会处理任何群。"
  },
  "exclude_groups": {
    "description": "排除群聊黑名单",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": [],
    "hint": "黑名单中的群聊将保持 AstrBot 默认处理流程，本插件不介入。"
  },
  "provider": {
    "description": "旧版统一 Chat Provider（兼容项）",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "兼容 v1.1：当 reply_provider/vibe_provider 为空时同时作为主回复和氛围校准 Provider。新配置请优先填写专用字段。"
  },
  "reply_provider": {
    "description": "主回复 Chat Provider",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "为空时回退旧版 provider，再为空时按当前 UMO 使用会话默认 Provider。"
  },
  "vibe_provider": {
    "description": "氛围校准 Chat Provider",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "仅用于低频氛围校准；为空时回退旧版 provider。persona_model 下不会再调用氛围 LLM，此字段失效。"
  },
  "bot_names": {
    "description": "机器人昵称 / 唤醒名",
    "type": "list",
    "items": {
      "type": "string"
    },
    "default": [],
    "hint": "用于识别点名。请填写完整昵称（如“小助手”），不要用“bot”“助手”这种容易误伤的短词。"
  },
  "command_prefix": {
    "description": "指令前缀",
    "type": "string",
    "default": "/",
    "hint": "以此前缀开头的消息不会被状态机吞掉，交给指令处理器。"
  },
  "debounce_base_cooldown": {
    "description": "防抖基础冷却时间 (秒)",
    "type": "float",
    "default": 3.5,
    "slider": {
      "min": 0,
      "max": 30,
      "step": 0.1
    },
    "hint": "同一用户发送消息后的基础等待时间（默认 3.5 秒）。"
  },
  "debounce_extended_cooldown": {
    "description": "悬挂语句扩展冷却时间 (秒)",
    "type": "float",
    "default": 6.5,
    "slider": {
      "min": 0,
      "max": 60,
      "step": 0.1
    },
    "hint": "当检测到语句截断或未闭合标点时动态拉伸的冷却时间（默认 6.5 秒）。"
  },
  "debounce_max_cap": {
    "description": "防抖硬上限时间 (秒)",
    "type": "float",
    "default": 12.0,
    "slider": {
      "min": 0,
      "max": 120,
      "step": 0.5
    },
    "hint": "单次聚合窗口的最长允许持续时间，防止无限推迟（默认 12.0 秒）。"
  },
  "strong_addressivity_threshold": {
    "description": "强指代判定阈值",
    "type": "float",
    "default": 0.7,
    "slider": {
      "min": 0,
      "max": 1,
      "step": 0.05
    },
    "hint": "定向度评分大于等于该阈值时判定为明确指向机器人（默认 0.70）。"
  },
  "safe_hover_threshold": {
    "description": "安全悬停判定阈值",
    "type": "float",
    "default": 0.4,
    "slider": {
      "min": 0,
      "max": 1,
      "step": 0.05
    },
    "hint": "定向度在此区间内 (0.40 ~ 0.70) 仅静默记入图谱，不主动抢答（默认 0.40）。"
  },
  "deep_cooling_minutes": {
    "description": "深度冷却周期 (分钟)",
    "type": "float",
    "default": 15.0,
    "slider": {
      "min": 0,
      "max": 180,
      "step": 1
    },
    "hint": "触发话题壁垒或冷场后，机器人保持静默观察的分钟数（默认 15.0 分钟）。"
  },
  "chars_per_second": {
    "description": "模拟打字速率 (字/秒)",
    "type": "float",
    "default": 25.0,
    "slider": {
      "min": 1,
      "max": 100,
      "step": 1
    },
    "hint": "用于计算拟人化输入延迟的打字速度（默认 25.0 字/秒）。"
  },
  "base_thinking_delay": {
    "description": "基础思考停顿 (秒)",
    "type": "float",
    "default": 0.8,
    "slider": {
      "min": 0,
      "max": 10,
      "step": 0.1
    },
    "hint": "首次输出前的拟人化思考停顿（默认 0.8 秒）。"
  },
  "max_fragments": {
    "description": "最大碎发条数",
    "type": "int",
    "default": 3,
    "slider": {
      "min": 1,
      "max": 3,
      "step": 1
    },
    "hint": "装饰原管线回复时最多拆成几条。超过 3 没有意义，硬上限就是 3。"
  },
  "max_fragment_chars": {
    "description": "单条消息目标长度",
    "type": "int",
    "default": 120,
    "slider": {
      "min": 40,
      "max": 500,
      "step": 10
    },
    "hint": "碎发时单条消息的软长度目标；极长回复会优先保留完整内容。"
  },
  "inter_burst_interval": {
    "description": "碎发基础间隔 (秒)",
    "type": "float",
    "default": 1.2,
    "slider": {
      "min": 0.6,
      "max": 3,
      "step": 0.1
    },
    "hint": "连续短消息之间的基础间隔，会按消息长度和群聊模式微调。"
  },
  "casual_emoji_enabled": {
    "description": "允许闲聊表情反应",
    "type": "bool",
    "default": false,
    "hint": "legacy 下仅在非正式短回复末尾追加一个场景匹配的认可 Emoji。persona_model 下后处理不补表情，表情由带人设的生成决定。"
  },
  "strip_markdown_in_banter": {
    "description": "闲聊模式格式降级",
    "type": "bool",
    "default": true,
    "hint": "在快速碎梗与非正式闲聊氛围下自动清洗 Markdown 格式与生硬标点。"
  },
  "vibe_llm_enabled": {
    "description": "启用低频 LLM 氛围校准",
    "type": "bool",
    "default": false,
    "hint": "默认关闭。legacy 下可低频校准氛围。persona_model 下不会再调用氛围 LLM，本地遥测仍作为决策参考。"
  },
  "shadow_mode": {
    "description": "观察模式（不产生副作用）",
    "type": "bool",
    "default": false,
    "hint": "legacy：只记录预计动作，不抑制、停止、调用 LLM 或发送。persona_model：仍调用决策模型并记录预计动作/状态/目标/耗时，但不生成回复、不改宿主回复。"
  },
  "learning_policy_mode": {
    "description": "Dynamics Learning 策略消费模式",
    "type": "string",
    "default": "off",
    "options": [
      "off",
      "shadow",
      "active"
    ],
    "hint": "off：不读取学习层发布的策略（默认）。shadow：读取并解析、算出会改成什么，但不应用，只用于对比与收集分歧数据。active：应用，且必须通过发布协议版本、已验证本体版本、基线配置摘要三项检查；任何一项不通过都会拒绝而不是降级应用。"
  },
  "learning_policy_source_id": {
    "description": "学习层插件标识",
    "type": "string",
    "default": "ysyhlly/astrbot_plugin_dynamics_learning",
    "hint": "Dynamics Learning 在共享首选项里的作用域标识，格式 author/name（均小写）。它把发布契约写在 learning_published_v1 这一个键里，本插件只读、从不写入。"
  },
  "learning_policy_expected_policy_id": {
    "description": "锁定策略 ID（可选）",
    "type": "string",
    "default": "",
    "hint": "填了就只接受这一个 policy_id，别的版本一律拒绝。这是「人为批准锁定」的正确位置——管理员表达的是「我批准运行 policy X」，而不是批准训练它的那批数据。留空表示接受最新的 promoted 策略。"
  },
  "learning_policy_expected_dataset_fingerprint": {
    "description": "锁定数据指纹（可选）",
    "type": "string",
    "default": "",
    "hint": "默认留空：数据指纹是来源证明，不是运行时兼容条件，本插件不会因为它缺失或变化而拒绝策略。只有在这里显式填写时才做完全匹配校验。"
  },
  "learning_policy_refresh_seconds": {
    "description": "学习策略刷新间隔（秒）",
    "type": "int",
    "default": 60,
    "hint": "读取发布文件的最小间隔。默认 60 秒：另一个插件的共享首选项不该出现在每条群消息的热路径上。范围 10~3600。"
  },
  "console_show_message_content": {
    "description": "控制台显示消息正文",
    "type": "bool",
    "default": false,
    "hint": "默认关闭以减少群聊内容暴露；开启后管理员控制台详情会显示截断后的 DAG 和最近回复正文。"
  },
  "annotation_draft_enabled": {
    "description": "AI 预标注（草稿）",
    "type": "bool",
    "default": false,
    "hint": "默认关闭。开启后可在回放页外层一键生成，也可另行开启定时生成。生成时会把会话消息正文发给预标注模型，起草「该不该回 / 是否在对机器人说话」。草稿仍需在回放或审批页人工采纳，记录会保留 AI 草稿来源。每条草稿会连它对应的消息一起保存（路由、决策轨迹、时间；正文仅在开启控制台消息内容显示时保存），这样重启或消息超出保留窗口后仍可采纳。"
  },
  "annotation_draft_provider": {
    "description": "AI 预标注 Chat Provider",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "留空时回退旧版 provider，再为空时按会话默认 Provider。预标注是一次离线判读，建议固定一个便宜且稳定的模型。"
  },
  "annotation_draft_auto_enabled": {
    "description": "定时生成 AI 标注草稿",
    "type": "bool",
    "default": false,
    "hint": "需同时开启 AI 预标注。按设定间隔依次检查生效群的会话，把未标注且没有草稿的消息正文发给预标注模型；已有草稿不重复生成。只生成待审草稿，仍需人工采纳。"
  },
  "annotation_draft_interval_minutes": {
    "description": "自动生成间隔（分钟）",
    "type": "float",
    "default": 15.0,
    "slider": { "min": 1, "max": 1440, "step": 1 },
    "hint": "范围 1–1440 分钟，默认 15 分钟。开启或修改间隔后等待一个间隔开始；各会话依次生成，上轮未完成时不会叠加新一轮。每次仍受单次预标注条数限制。"
  },
  "annotation_draft_limit": {
    "description": "单次预标注条数",
    "type": "int",
    "default": 20,
    "hint": "一次为多少条未标注消息起草（范围 1~40）。条数越多，一次调用越贵，草稿之间也更容易互相影响。"
  },
  "annotation_draft_timeout": {
    "description": "预标注超时 (秒)",
    "type": "float",
    "default": 60.0,
    "slider": {
      "min": 10,
      "max": 300,
      "step": 5
    },
    "hint": "一次预标注要读整段窗口，比单条回复慢；超时即放弃，不写任何草稿。范围 10~300。"
  },
  "telemetrics_window_seconds": {
    "description": "遥测滑动窗口 (秒)",
    "type": "float",
    "default": 60.0,
    "slider": {
      "min": 15,
      "max": 300,
      "step": 5
    },
    "hint": "计算 MPM、平均字符数和场景标签时使用的时间窗口（默认 60 秒）。"
  },
  "fast_banter_enter_mpm": {
    "description": "进入快速碎梗的 MPM",
    "type": "float",
    "default": 12.0,
    "slider": {
      "min": 4,
      "max": 40,
      "step": 0.5
    },
    "hint": "消息速率达到该阈值时，氛围状态机更倾向进入 fast_banter。"
  },
  "chill_fade_enter_mpm": {
    "description": "进入能量衰退的 MPM",
    "type": "float",
    "default": 3.0,
    "slider": {
      "min": 0.5,
      "max": 12,
      "step": 0.5
    },
    "hint": "消息速率低于该阈值时，氛围状态机更倾向进入 chill_fade。"
  },
  "wts_topic_weight": {
    "description": "WTS 话题相关度权重",
    "type": "float",
    "default": 0.12,
    "slider": {
      "min": 0,
      "max": 0.5,
      "step": 0.01
    },
    "hint": "legacy 环境插话时，话题相关度对发言欲望的加分权重。persona_model 下不单独否决是否开口，仅作决策参考。"
  },
  "wts_professionalism_weight": {
    "description": "WTS 专业度权重",
    "type": "float",
    "default": 0.08,
    "slider": {
      "min": 0,
      "max": 0.5,
      "step": 0.01
    },
    "hint": "legacy 下内容专业度对发言欲望的加分权重。persona_model 下不单独否决是否开口。"
  },
  "wts_question_weight": {
    "description": "WTS 问题价值权重",
    "type": "float",
    "default": 0.08,
    "slider": {
      "min": 0,
      "max": 0.5,
      "step": 0.01
    },
    "hint": "legacy 下问句或求助句对发言欲望的加分权重。persona_model 下不单独否决是否开口。"
  },
  "wts_participation_weight": {
    "description": "WTS 多人参与度权重",
    "type": "float",
    "default": 0.06,
    "slider": {
      "min": 0,
      "max": 0.5,
      "step": 0.01
    },
    "hint": "legacy 下窗口内独立发言人数对发言欲望的加分权重。persona_model 下不单独否决是否开口。"
  },
  "wts_fatigue_weight": {
    "description": "WTS 发言疲劳权重",
    "type": "float",
    "default": 1.0,
    "slider": {
      "min": 0,
      "max": 2,
      "step": 0.05
    },
    "hint": "legacy 下五分钟内累计发言对发言欲望的惩罚倍率。persona_model 下不因连续短句强制冷却。"
  },
  "conversation_router_enabled": {
    "description": "启用短期会话路由",
    "type": "bool",
    "default": true,
    "hint": "结合活跃话题、候选父消息和对话对象判断自然续聊，区分谈论机器人与向机器人提问。"
  },
  "topic_reranker_enabled": {
    "description": "启用话题 LLM 聚合辅助",
    "type": "bool",
    "default": true,
    "hint": "对模糊归属和拟新建话题调用模型，识别同一讨论的回答、补充和子问题；失败保留本地判断。"
  },
  "topic_reranker_provider": {
    "description": "话题复判模型",
    "type": "string",
    "default": "",
    "_special": "select_provider",
    "hint": "选择模型提供商；留空使用当前会话模型。"
  },
  "topic_reranker_timeout": {
    "description": "话题复判超时（秒）",
    "type": "float",
    "default": 3.0,
    "slider": {
      "min": 0.1,
      "max": 10,
      "step": 0.1
    }
  },
  "routing_neural_timeout": {
    "description": "模糊消息向量等待上限（秒）",
    "type": "float",
    "default": 0.5,
    "slider": {
      "min": 0,
      "max": 2,
      "step": 0.1
    },
    "hint": "仅启用神经向量时生效；明确 @ 或引用不等待。超时使用本地证据，0 表示不等待。"
  },
  "replay_message_limit": {
    "description": "每会话回放消息上限",
    "type": "int",
    "default": 500,
    "slider": {
      "min": 80,
      "max": 500,
      "step": 20
    },
    "hint": "场景回放每个会话最多展示的最近消息数，包含已归属和未归属消息。实际受会话图谱 500 条及一小时保留期限制；调小只减少展示，不删除已保存消息。"
  },
  "topic_window_seconds": {
    "description": "活跃话题保留时间（秒）",
    "type": "float",
    "default": 300.0,
    "slider": {
      "min": 60.0,
      "max": 1800.0,
      "step": 30.0
    },
    "hint": "会话话题活跃窗口。超时无新消息的话题会被自动清理，避免历史话题错误复用。"
  },
  "topic_join_threshold": {
    "description": "话题阈值兼容设置",
    "type": "float",
    "default": 0.48,
    "slider": {
      "min": 0.3,
      "max": 0.85,
      "step": 0.02
    },
    "hint": "保留旧配置行为：作为歧义候选阈值；小于 0.58 时，确定归属阈值为 max(0.58, 此值 + 0.10)，否则等于此值。下方独立阈值非 0 时分别覆盖对应设置。"
  },
  "topic_commit_threshold": {
    "description": "话题确定归属阈值",
    "type": "float",
    "default": 0.0,
    "hint": "0 表示沿用兼容设置的旧映射；手动设置范围 0.30～0.95。匹配得分达到此值且满足领先间隔，才确定归入已有话题。"
  },
  "topic_ambiguity_threshold": {
    "description": "话题歧义候选阈值",
    "type": "float",
    "default": 0.0,
    "hint": "0 表示沿用话题阈值兼容设置；手动设置范围 0.30～0.95。控制需要保留并进一步判别的候选话题，不直接决定归属。"
  },
  "topic_margin_threshold": {
    "description": "话题确定归属领先间隔",
    "type": "float",
    "default": 0.06,
    "slider": {
      "min": 0.0,
      "max": 0.5,
      "step": 0.01
    },
    "hint": "最佳候选相对第二候选的最低得分差。与确定归属阈值同时满足，才接受话题归属。"
  },
  "parent_window_seconds": {
    "description": "候选父消息检索窗口（秒）",
    "type": "float",
    "default": 180.0,
    "slider": {
      "min": 30.0,
      "max": 600.0,
      "step": 15.0
    },
    "hint": "在当前话题内向前寻找最可能被回复/提问的父消息的时间跨度。"
  },
  "parent_accept_threshold": {
    "description": "推断回复边接受阈值",
    "type": "float",
    "default": 0.72,
    "slider": {
      "min": 0.5,
      "max": 0.95,
      "step": 0.02
    },
    "hint": "多因子打分达到该阈值且显著领先（margin >= 0.08）时，才建立推断回复边 (inferred_reply)。"
  },
  "neural_embedding_enabled": {
    "description": "启用神经网络 Embedding",
    "type": "bool",
    "default": false,
    "hint": "开启后通过 AstrBot Embedding Provider 计算语义向量。未配置或调用失败时自动回退到本地哈希 embedding。"
  },
  "embedding_provider": {
    "description": "Embedding Provider ID",
    "type": "string",
    "default": "",
    "hint": "在插件「参数配置」页从 Embedding 服务商下拉选择；留空则使用第一个可用的 Embedding Provider。原生设置页请填写 Embedding Provider ID。"
  },
  "neural_link_threshold": {
    "description": "神经网络语义边阈值",
    "type": "float",
    "default": 0.78,
    "slider": {
      "min": 0.5,
      "max": 0.95,
      "step": 0.01
    },
    "hint": "两侧都已缓存神经网络向量时，余弦相似度达到该阈值会建立语义候选边。"
  },
  "embedding_cache_size": {
    "description": "Embedding 缓存条数",
    "type": "int",
    "default": 512,
    "slider": {
      "min": 64,
      "max": 4096,
      "step": 64
    },
    "hint": "进程内 LRU 缓存的文本向量条数，避免对同一句话重复请求 Embedding API。"
  },
  "embedding_cache_ttl_seconds": {
    "description": "Embedding 缓存有效期（秒）",
    "type": "int",
    "default": 1800,
    "slider": {
      "min": 0,
      "max": 7200,
      "step": 300
    },
    "hint": "超过该时长未再使用的文本向量会被回收，0 表示不按时间回收。CD 只保留短期工作记忆，长期记忆由记忆搭档负责。"
  },
  "presence_knob": {
    "description": "分寸旋钮（存在感）",
    "type": "string",
    "default": "sensible",
    "options": [
      "ghost",
      "sensible",
      "lively"
    ],
    "hint": "隐身(ghost)：主要回应点名；懂事(sensible)：按需参与；活跃(lively)：更主动参与公开讨论。人设决策会读取档位，懂事/活跃每会话每分钟最多主动加入 2/4 次，明确点名与自然续聊不占此额度；仍遵守冷却、作息和对话边界。立即生效。"
  },
  "social_manners_enabled": {
    "description": "启用社交分寸总开关",
    "type": "bool",
    "default": true,
    "hint": "关闭后接力棒/谁的场/捧场配额都不生效。默认开启（懂事档）。"
  },
  "relay_baton_enabled": {
    "description": "接力棒：人类互回时靠边",
    "type": "bool",
    "default": true,
    "hint": "两人正在互回时不插话；冷场、点名缺口或 @ 才轻接。"
  },
  "private_field_enabled": {
    "description": "谁的场：A↔B 私场不硬插",
    "type": "bool",
    "default": true,
    "hint": "误判偏保守。公开整活、@bot、明确求助除外。"
  },
  "hyped_quota_enabled": {
    "description": "捧场配额：整活最多起哄一次",
    "type": "bool",
    "default": true,
    "hint": "同一段整活最多起哄一次；用完改旁听；被点名可回但不叠梗。"
  },
  "mood_memory_enabled": {
    "description": "情绪轻接（话题情绪记忆）",
    "type": "bool",
    "default": false,
    "hint": "默认关闭。短标签+衰减；有 selflearning 时优先消费已批准记忆。"
  },
  "slang_trial_enabled": {
    "description": "黑话试用期",
    "type": "bool",
    "default": false,
    "hint": "默认关闭。新梗小声用一次，冷场收回；未审查/敏感不进场。"
  },
  "group_memory_enabled": {
    "description": "群记忆小本（纪念日/约定）",
    "type": "bool",
    "default": true,
    "hint": "默认开启，但须显式写入纪念日或约定才有效。"
  },
  "selflearning_hub_url": {
    "description": "Self Learning Hub v1 地址（可选）",
    "type": "string",
    "default": "",
    "hint": "仅供不经过宿主请求钩子的内部回复使用；普通聊天由原生钩子增强，不重复调用 context。填写 Self Learning HTTP 服务地址或 /api/hub/v1 地址，留空关闭 Hub 连接。配置了密钥时仅接受 https（本机回环可用 http），避免凭据明文外发；其余情况按降级处理并在诊断里显示原因。Hub 需在 /context 响应里回显 group_id 与 user_id，缺失或不一致时本次背景数据会被丢弃（scope_mismatch）。"
  },
  "selflearning_hub_key_env": {
    "description": "Hub API Key 环境变量名",
    "type": "string",
    "default": "SELFLEARNING_HUB_API_KEY",
    "hint": "填写运行 AstrBot 的进程环境变量名，不是密钥本身；凭据只从进程环境读取，不返回面板。Self Learning 未开启 API 鉴权时可留空。为免任意环境变量被当作凭据外发，变量名须是 Hub/本插件相关名称（含 HUB、SELFLEARNING 或 CHAT_DYNAMICS），否则回退默认值并在配置提示中说明。"
  },
  "selflearning_integration": {
    "description": "尝试对接 selflearning",
    "type": "bool",
    "default": true,
    "hint": "有则增强记忆/关系/黑话候选；检测失败安静降级，不阻断回复链路。"
  },
  "media_image_gate_enabled": {
    "description": "会看图（门闩）",
    "type": "bool",
    "default": true,
    "hint": "见到图片/表情时用短标签判断能不能插话与力道；不默认看图答题。失败安静降级。"
  },
  "media_voice_gate_enabled": {
    "description": "会听语音（门闩）",
    "type": "bool",
    "default": true,
    "hint": "见到语音时用短标签判断开不开口；不做每条转写。短敷衍旁听，求助口吻才更愿意接。"
  },
  "media_understand_reply_enabled": {
    "description": "允许理解接话（默认关）",
    "type": "bool",
    "default": false,
    "hint": "L2：未 @ 时，仅「看/听这个」等强相关才把图/语音交主 Agent。@ 或引用 bot 时始终交模型识图/听语音（隐私档除外）。宿主无多模态时安静降级。"
  },
  "media_privacy_strict": {
    "description": "媒体隐私严格档",
    "type": "bool",
    "default": true,
    "hint": "证件/私密感图片不描述细节；未强点名时优先沉默。"
  },
  "deciding_detect_enabled": {
    "description": "识别决策中场合",
    "type": "bool",
    "default": true,
    "hint": "约时间/投票/选型/分工时进入决策中：不插科、短澄清；不确定不当决策态。"
  },
  "gap_fill_proactive_enabled": {
    "description": "缺口补全主动",
    "type": "bool",
    "default": true,
    "hint": "悬空问句、约定缺口、求助后沉默等白名单才主动；不是怕冷场找话。"
  },
  "cold_memory_nudge_enabled": {
    "description": "冷场公共记忆轻唤",
    "type": "bool",
    "default": true,
    "hint": "开关默认开，但仅活跃档(lively)生效；无记忆小本不编群史。"
  },
  "newcomer_caution_enabled": {
    "description": "新人更收",
    "type": "bool",
    "default": true,
    "hint": "新/低频发言者提高插话门槛；禁止环境主动点名；误判偏收。"
  },
  "pace_align_enabled": {
    "description": "节奏对齐",
    "type": "bool",
    "default": true,
    "hint": "跟群节奏调延迟与片段长度；不单独授权更多主动。"
  },
  "proactive_quota_enabled": {
    "description": "主动配额",
    "type": "bool",
    "default": true,
    "hint": "规则模式（decision_mode=legacy）的每小时/每话题段上限，用尽后纯响应（只答 @/强相关）。人设模式由决策模型与每分钟开口预算控制，本组设置不参与人设模式判定。"
  },
  "proactive_quota_per_hour": {
    "description": "每小时主动上限",
    "type": "int",
    "default": 2,
    "slider": {
      "min": 0,
      "max": 20,
      "step": 1
    },
    "hint": "偏紧默认 2。用尽后只答点名/强相关。仅作用于规则模式。"
  },
  "proactive_quota_per_topic": {
    "description": "每话题段主动上限",
    "type": "int",
    "default": 1,
    "slider": {
      "min": 0,
      "max": 10,
      "step": 1
    },
    "hint": "同一话题段默认最多主动 1 次。仅作用于规则模式。"
  },
  "daily_rhythm_enabled": {
    "description": "今日作息总开关",
    "type": "bool",
    "default": true,
    "hint": "氛围向作息：日间可早安/少量分享；夜晚收束中≠已睡。非真实日历。"
  },
  "rhythm_timezone": {
    "description": "今日作息时区",
    "type": "string",
    "default": "",
    "hint": "填写 IANA 时区（如 Asia/Shanghai）；留空沿用服务器本地时区。无效值回退本地时区并告警。"
  },
  "rhythm_morning_hi_enabled": {
    "description": "醒来可早安",
    "type": "bool",
    "default": true,
    "hint": "醒来窗可选问好；隐身/冲突/私场/决策中不主动早安。"
  },
  "rhythm_day_share_slots": {
    "description": "白天分享槽",
    "type": "int",
    "default": 1,
    "slider": {
      "min": 0,
      "max": 2,
      "step": 1
    },
    "hint": "0～2 槽/日，耗主动配额；优先缺口/公共记忆，禁硬编炫耀日程。"
  },
  "rhythm_goodnight_text_quota": {
    "description": "当晚文字晚安配额",
    "type": "int",
    "default": 1,
    "slider": {
      "min": 1,
      "max": 2,
      "step": 1
    },
    "hint": "收束会话文字晚安相关默认 1，最多 2；首波可回，后续静默。"
  },
  "rhythm_sleep_after_winddown": {
    "description": "收束后再睡",
    "type": "bool",
    "default": true,
    "hint": "回晚安不得直接已睡；冷场约 20～40 分钟后再入睡。"
  },
  "rhythm_allow_self_sleep": {
    "description": "允许自己主动睡",
    "type": "bool",
    "default": true,
    "hint": "到睡点且已冷/收束完成可无声入睡；热聊推迟。"
  },
  "rhythm_allow_wake": {
    "description": "允许被吵醒",
    "type": "bool",
    "default": true,
    "hint": "@/引用睡前/强求助/媒体向 bot/命令 → 短回后短醒冷却再睡。"
  },
  "rhythm_insomnia_enabled": {
    "description": "允许睡不着主动",
    "type": "bool",
    "default": false,
    "hint": "默认关。开启后极低概率+周期硬顶，至多一句气氛点缀。"
  },
  "rhythm_force_sleep": {
    "description": "强制入睡",
    "type": "bool",
    "default": false,
    "hint": "覆盖状态：环境主动=0；普通晚安不吵醒。"
  },
  "rhythm_skip_morning_hi_tonight": {
    "description": "今晚别早安",
    "type": "bool",
    "default": false,
    "hint": "今晚跳过醒来问好。"
  }
}
